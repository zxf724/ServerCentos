#include <stdio.h>
#include <stdint.h>

uint8_t step(uint8_t step) {
  for(uint8_t i;i<= step;i++) {
    for(uint8_t j;j<=100;j++) {
    }
  }
  return true;
}

void main() {
  uint8_t step = 20;
  if(step(step)) {
  printf("hello,world!");
}
